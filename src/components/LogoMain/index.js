import styled from 'styled-components';

import Logo from '../../assets/img/Logo.png';

const LogoMain = styled.img.attrs({ src: Logo, alt: 'Logo da ComeDevUp'})`
    height: 40px;
`;

export default LogoMain;